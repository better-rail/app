diff --git a/build/getDevicePushTokenAsync.js b/build/getDevicePushTokenAsync.js
index f0875c5eaa84d45d646edd1ad5f21a962f68ab02..eea98fba79369545af2658b54051244bb1947eb2 100644
--- a/build/getDevicePushTokenAsync.js
+++ b/build/getDevicePushTokenAsync.js
@@ -12,16 +12,20 @@ export async function getDevicePushTokenAsync() {
         throw new UnavailabilityError('ExpoNotifications', 'getDevicePushTokenAsync');
     }
     warnOfExpoGoPushUsage();
+    // Reuse an in-flight request, but never retain a settled promise. In particular, a rejected
+    // promise must be cleared so a transient native registration failure can be retried.
+    const tokenPromise = nativeTokenPromise ?? PushTokenManager.getDevicePushTokenAsync();
+    nativeTokenPromise = tokenPromise;
     let devicePushToken;
-    if (nativeTokenPromise) {
-        // Reuse existing Promise
-        devicePushToken = await nativeTokenPromise;
+    try {
+        devicePushToken = await tokenPromise;
     }
-    else {
-        // Create a new Promise and clear it afterwards
-        nativeTokenPromise = PushTokenManager.getDevicePushTokenAsync();
-        devicePushToken = await nativeTokenPromise;
-        nativeTokenPromise = null;
+    finally {
+        // A second request may have started after this one settled. Only clear the promise that this
+        // invocation actually awaited so the newer request remains deduplicated.
+        if (nativeTokenPromise === tokenPromise) {
+            nativeTokenPromise = null;
+        }
     }
     // @ts-ignore: TS thinks Platform.OS could be anything and can't decide what type is it
     return { type: Platform.OS, data: devicePushToken };
diff --git a/src/getDevicePushTokenAsync.ts b/src/getDevicePushTokenAsync.ts
index 2ed08923e84fcc2dbcb0e4295bf5f1198cc60e58..012191127a3e81271eda9e4f2394c5b637d62a3a 100644
--- a/src/getDevicePushTokenAsync.ts
+++ b/src/getDevicePushTokenAsync.ts
@@ -17,15 +17,20 @@ export async function getDevicePushTokenAsync(): Promise<DevicePushToken> {
   }
   warnOfExpoGoPushUsage();
-
+
+  // Reuse an in-flight request, but never retain a settled promise. In particular, a rejected
+  // promise must be cleared so a transient native registration failure can be retried.
+  const tokenPromise = nativeTokenPromise ?? PushTokenManager.getDevicePushTokenAsync();
+  nativeTokenPromise = tokenPromise;
+
   let devicePushToken: string | null;
-  if (nativeTokenPromise) {
-    // Reuse existing Promise
-    devicePushToken = await nativeTokenPromise;
-  } else {
-    // Create a new Promise and clear it afterwards
-    nativeTokenPromise = PushTokenManager.getDevicePushTokenAsync();
-    devicePushToken = await nativeTokenPromise;
-    nativeTokenPromise = null;
+  try {
+    devicePushToken = await tokenPromise;
+  } finally {
+    // A second request may have started after this one settled. Only clear the promise that this
+    // invocation actually awaited so the newer request remains deduplicated.
+    if (nativeTokenPromise === tokenPromise) {
+      nativeTokenPromise = null;
+    }
   }
-
+
   // @ts-ignore: TS thinks Platform.OS could be anything and can't decide what type is it
