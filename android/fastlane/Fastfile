opt_out_usage

default_platform(:android)

platform :android do
  desc "Fastlane test"
  lane :deploy_internal do
    gradle(task: "clean")
    gradle(
        task: 'bundle',
        build_type: 'Release'
    ) 

    upload_to_play_store(
      track: 'internal'
    )
  end

end
